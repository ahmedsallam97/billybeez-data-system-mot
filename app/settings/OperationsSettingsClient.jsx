"use client";

import { useCallback, useEffect, useMemo, useState } from "react";
import Link from "next/link";
import { useI18n } from "../i18n";

async function read(response) { const text = await response.text(); const data = text ? JSON.parse(text) : {}; if (!response.ok) throw new Error(data.error || "Request failed"); return data; }
function Field({ label, children }) { return <label className="ops-setting-field"><span>{label}</span>{children}</label>; }

export default function OperationsSettingsClient() {
  const { isArabic } = useI18n();
  const [tab, setTab] = useState("insights");
  const [config, setConfig] = useState(null);
  const [summary, setSummary] = useState(null);
  const [health, setHealth] = useState(null);
  const [message, setMessage] = useState("");
  const [busy, setBusy] = useState(false);
  const [search, setSearch] = useState("");
  const load = useCallback(async () => {
    setMessage("");
    try {
      const [configuration, insights, systemHealth] = await Promise.all([
        fetch("/api/operations/config", { cache: "no-store" }).then(read),
        fetch("/api/operations/summary", { cache: "no-store" }).then(read),
        fetch("/api/operations/health", { cache: "no-store" }).then(read),
      ]);
      setConfig(configuration); setSummary(insights); setHealth(systemHealth);
    } catch (error) { setMessage(error.message); }
  }, []);
  useEffect(() => { load(); }, [load]);

  async function saveSetting(key, value, notice) {
    setBusy(true); setMessage("");
    try { await fetch("/api/settings", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ key, value: JSON.stringify(value) }) }).then(read); setMessage(notice || (isArabic ? "تم حفظ الإعدادات" : "Settings saved")); await load(); }
    catch (error) { setMessage(error.message); } finally { setBusy(false); }
  }
  async function saveRecord(action, form) {
    setBusy(true); setMessage("");
    try { const body = { action, ...Object.fromEntries(new FormData(form).entries()) }; body.critical = body.critical === "on"; body.requiresQualification = body.requiresQualification === "on"; await fetch("/api/operations/config", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(body) }).then(read); form.reset(); setMessage(isArabic ? "تم الحفظ" : "Saved"); await load(); }
    catch (error) { setMessage(error.message); } finally { setBusy(false); }
  }

  const groups = [
    ["insights", "01", isArabic ? "الإنسايتس" : "Insights", isArabic ? "قرارات اليوم ومساعد العمليات" : "Decisions and assistant"],
    ["roster", "02", isArabic ? "الروستر والشيفتات" : "Roster & shifts", isArabic ? "الأوقات والكاشير والمواقع" : "Times, cashiers and positions"],
    ["template", "03", isArabic ? "التيمبلت" : "Template", isArabic ? "الشكل والجمل والطباعة" : "Layout, phrases and print"],
    ["master", "04", isArabic ? "البيانات الأساسية" : "Master data", isArabic ? "الأكاديميات والعملاء والكتالوج" : "Academies, customers and catalogues"],
    ["rules", "05", isArabic ? "قواعد التشغيل" : "Operation rules", isArabic ? "الحضور والتقييم والإجازات" : "Attendance, evaluation and leave"],
    ["safety", "06", isArabic ? "السجل والسلامة" : "Audit & safety", isArabic ? "القاعدة والنسخ والتغييرات" : "Database, backups and changes"],
  ];
  const visibleGroups = groups.filter((item) => !search || `${item[2]} ${item[3]}`.toLowerCase().includes(search.toLowerCase()));
  if (!config) return <div className="manager-skeleton"><div className="card skeleton-card" /></div>;
  const settings = config.settings || {};
  return <section className="panel settings-shell settings-workbench operations-settings-center">
    <div className="settings-workbench-head"><div><span>{isArabic ? "مركز تحكم المدير" : "MANAGEMENT CONTROL CENTER"}</span><h1>{isArabic ? "إعدادات العمليات" : "Operations Settings"}</h1><p>{isArabic ? "القواعد والبيانات الأساسية والإنسايتس في مكان واحد." : "Rules, master data and actionable insights in one place."}</p></div><input className="settings-search" value={search} onChange={(event) => setSearch(event.target.value)} placeholder={isArabic ? "ابحث في الإعدادات" : "Search settings"} /></div>
    <div className="settings-group-strip">{visibleGroups.map(([key, code, title, hint]) => <button type="button" className={`settings-group-card tone-${key} ${tab === key ? "active" : ""}`} key={key} onClick={() => setTab(key)}><small>{code}</small><b>{title}</b><span>{hint}</span></button>)}</div>
    {message && <div className={`alert ${message.includes("تم") || message === "Saved" ? "success" : "danger"}`}>{message}</div>}
    <div className="settings-content">
      {tab === "insights" && <Insights summary={summary} health={health} config={config} isArabic={isArabic} />}
      {tab === "roster" && <RosterSettings config={config} settings={settings} isArabic={isArabic} busy={busy} saveSetting={saveSetting} saveRecord={saveRecord} />}
      {tab === "template" && <TemplateSettings settings={settings} isArabic={isArabic} busy={busy} saveSetting={saveSetting} />}
      {tab === "master" && <MasterData config={config} settings={settings} isArabic={isArabic} busy={busy} saveSetting={saveSetting} saveRecord={saveRecord} />}
      {tab === "rules" && <RulesSettings settings={settings} criteria={config.criteriaVersion} isArabic={isArabic} busy={busy} saveSetting={saveSetting} />}
      {tab === "safety" && <Safety health={health} isArabic={isArabic} reload={load} />}
    </div>
  </section>;
}

function Insights({ summary, health, config, isArabic }) {
  const quality = summary?.dataQuality || {};
  const attention = summary?.attention || [];
  const assistant = useMemo(() => {
    const lines = [];
    if (attention.length) lines.push(isArabic ? `يوجد ${attention.length} إجراء يحتاج قرارًا الآن.` : `${attention.length} actions need a decision now.`);
    if (summary?.planning?.upcomingBookings) lines.push(isArabic ? `يوجد ${summary.planning.upcomingBookings} حجزًا خلال السبعة أيام القادمة.` : `${summary.planning.upcomingBookings} bookings are due in the next seven days.`);
    if (quality.inconsistentActiveStatus) lines.push(isArabic ? `${quality.inconsistentActiveStatus} ملف موظف يحتاج توحيد حالة النشاط.` : `${quality.inconsistentActiveStatus} employee records have inconsistent active status.`);
    if (!lines.length) lines.push(isArabic ? "لا توجد مشكلة حرجة مسجلة؛ راجع التغطية قبل فتح اليوم." : "No critical issue is recorded; review coverage before opening the day.");
    return lines;
  }, [attention, summary, quality, isArabic]);
  const arabicMessages = { SCHEDULE_NOT_PUBLISHED: "لا يوجد جدول منشور يغطي اليوم.", ATTENDANCE_NOT_STARTED: "الحضور لم يبدأ لليوم.", MISSING_ATTENDANCE: "توجد سجلات حضور تحتاج مراجعة.", APPRAISALS_PENDING: "توجد تقييمات شهرية تنتظر المراجعة أو الاعتماد.", LEAVE_REQUESTS_PENDING: "توجد طلبات إجازة تنتظر القرار.", EMPLOYEE_STATUS_MISMATCH: "توجد ملفات موظفين بحالة نشاط غير متطابقة.", LOW_STOCK: "توجد أصناف وصلت إلى مستوى المخزون المنخفض." };
  return <div className="ops-settings-section"><header className="settings-content-head"><div><span>{isArabic ? "صورة القرار" : "DECISION VIEW"}</span><h2>{isArabic ? "إنسايتس لها إجراء" : "Actionable insights"}</h2><p>{isArabic ? "لا نعرض عدادًا إلا لو يقود لإجراء واضح." : "Every metric below leads to a concrete action."}</p></div></header>
    <div className="operations-insight-grid">{attention.length ? attention.map((item) => <article className={`operations-insight ${item.severity}`} key={item.code}><b>{isArabic ? "إجراء مطلوب" : (item.title || item.code)}</b><span>{isArabic ? (arabicMessages[item.code] || item.message) : item.message}</span>{item.target && <Link href={`/operations?tab=${item.target}`}>{isArabic ? "فتح الإجراء" : "Open action"}</Link>}</article>) : <article className="operations-insight clear"><b>{isArabic ? "الوضع مستقر" : "Clear"}</b><span>{isArabic ? "لا توجد قرارات معلقة مسجلة." : "No pending operational decisions."}</span></article>}</div>
    <section className="panel operations-ai-brief"><header><span>OPERATIONS AI ASSISTANT</span><h2>{isArabic ? "ملخص المدير" : "Manager brief"}</h2></header>{assistant.map((line) => <p key={line}>• {line}</p>)}<small>{isArabic ? "الملخص مبني على بيانات النظام المسجلة ويحتاج مراجعة المدير." : "This brief uses recorded system evidence and requires manager review."}</small></section>
    <div className="grid three metrics"><article className="card metric"><div className="label">{isArabic ? "تغطية اليوم" : "Today coverage"}</div><div className="value">{summary?.currentSchedule ? (isArabic ? "منشور" : "Published") : (isArabic ? "غير منشور" : "Missing")}</div></article><article className="card metric"><div className="label">{isArabic ? "جودة بيانات الموظفين" : "Employee data quality"}</div><div className="value">{quality.inconsistentActiveStatus || 0}</div></article><article className="card metric"><div className="label">SQLite</div><div className="value">{health?.database?.integrity || "…"}</div></article></div>
  </div>;
}

function RosterSettings({ config, settings, isArabic, busy, saveSetting, saveRecord }) {
  const [cashier, setCashier] = useState(settings.OPS_CASHIER_CONFIG || { primaryEmployeeIds: [], backupEmployeeIds: [] });
  const [branch, setBranch] = useState(settings.OPS_BRANCH_CONFIG || { branchCode: "MOT", branchName: "MOT Branch", timezone: "Africa/Cairo" });
  const [codes, setCodes] = useState(settings.OPS_SCHEDULE_CODE_CONFIG || {});
  return <div className="ops-settings-section"><h2>{isArabic ? "الروستر والشيفتات" : "Roster and shifts"}</h2>
    <section className="panel form-grid"><h3>{isArabic ? "الفرع والمنطقة الزمنية" : "Branch and timezone"}</h3><Field label={isArabic ? "كود الفرع" : "Branch code"}><input value={branch.branchCode || ""} onChange={(e) => setBranch({ ...branch, branchCode: e.target.value })} /></Field><Field label={isArabic ? "اسم الفرع" : "Branch name"}><input value={branch.branchName || ""} onChange={(e) => setBranch({ ...branch, branchName: e.target.value })} /></Field><Field label={isArabic ? "المنطقة الزمنية" : "Timezone"}><input value={branch.timezone || ""} onChange={(e) => setBranch({ ...branch, timezone: e.target.value })} /></Field><button disabled={busy} onClick={() => saveSetting("OPS_BRANCH_CONFIG", branch)}>{isArabic ? "حفظ بيانات الفرع" : "Save branch"}</button></section>
    <section className="grid two"><article className="panel"><h3>{isArabic ? "مواعيد الشيفتات" : "Shift times"}</h3>{config.shifts.map((shift) => <form className="ops-inline-setting" key={shift.id} onSubmit={(event) => { event.preventDefault(); saveRecord("saveShift", event.currentTarget); }}><input name="code" defaultValue={shift.code} readOnly /><input name="label" defaultValue={shift.label} /><input name="labelAr" defaultValue={shift.labelAr || ""} /><input name="startTime" type="time" defaultValue={shift.startTime} /><input name="endTime" type="time" defaultValue={shift.endTime} /><button disabled={busy}>{isArabic ? "حفظ" : "Save"}</button></form>)}</article>
      <article className="panel"><h3>{isArabic ? "الكاشير الأساسي والاحتياطي" : "Primary and backup cashiers"}</h3>{config.employees.map((employee) => <div className="row" key={employee.id}><b>{employee.name}</b><span><label><input type="checkbox" checked={cashier.primaryEmployeeIds?.includes(employee.id)} onChange={(event) => setCashier((current) => ({ ...current, primaryEmployeeIds: event.target.checked ? [...new Set([...(current.primaryEmployeeIds || []), employee.id])] : (current.primaryEmployeeIds || []).filter((id) => id !== employee.id) }))} /> {isArabic ? "أساسي" : "Primary"}</label> <label><input type="checkbox" checked={cashier.backupEmployeeIds?.includes(employee.id)} onChange={(event) => setCashier((current) => ({ ...current, backupEmployeeIds: event.target.checked ? [...new Set([...(current.backupEmployeeIds || []), employee.id])] : (current.backupEmployeeIds || []).filter((id) => id !== employee.id) }))} /> {isArabic ? "احتياطي" : "Backup"}</label></span></div>)}<button disabled={busy} onClick={() => saveSetting("OPS_CASHIER_CONFIG", cashier)}>{isArabic ? "حفظ ترتيب الكاشير" : "Save cashier order"}</button></article></section>
    <section className="panel"><h3>{isArabic ? "ألوان حالات الجدول" : "Schedule code colors"}</h3><div className="settings-compact-grid">{Object.entries(codes).map(([code, meta]) => <article key={code}><b>{meta.label || code}</b><input aria-label={`${code} color`} type="color" value={meta.color || "#cccccc"} onChange={(event) => setCodes((current) => ({ ...current, [code]: { ...current[code], color: event.target.value } }))} /></article>)}</div><button disabled={busy} onClick={() => saveSetting("OPS_SCHEDULE_CODE_CONFIG", codes)}>{isArabic ? "حفظ الألوان" : "Save colors"}</button></section>
    <section className="panel"><h3>{isArabic ? "المواقع التشغيلية" : "Operational positions"}</h3><div className="settings-compact-grid">{config.positions.map((position) => <article key={position.id}><b>{position.labelAr || position.label}</b><span>{position.code}</span><small>{position.critical ? (isArabic ? "إلزامي" : "Mandatory") : (isArabic ? "مرن" : "Flexible")}</small></article>)}</div><form className="ops-inline-setting" onSubmit={(event) => { event.preventDefault(); saveRecord("savePosition", event.currentTarget); }}><input name="code" placeholder="Code" required /><input name="label" placeholder="English label" /><input name="labelAr" placeholder="الاسم العربي" /><label><input name="critical" type="checkbox" /> {isArabic ? "إلزامي" : "Mandatory"}</label><label><input name="requiresQualification" type="checkbox" defaultChecked /> {isArabic ? "يتطلب تأهيل" : "Qualification required"}</label><button>{isArabic ? "إضافة / تحديث" : "Add / update"}</button></form></section>
    <section className="panel"><h3>{isArabic ? "الأسماء الثنائية" : "Two-name roster labels"}</h3>{config.employees.map((employee) => <form className="ops-inline-setting" key={employee.id} onSubmit={(event) => { event.preventDefault(); saveRecord("saveOperationalName", event.currentTarget); }}><input type="hidden" name="employeeId" value={employee.id} /><b>{employee.name}</b><input name="operationalName" defaultValue={employee.operationalName || ""} placeholder={isArabic ? "الاسم الظاهر" : "Roster name"} /><select name="gender" defaultValue={employee.gender || ""}><option value="">{isArabic ? "النوع غير محدد" : "Gender not set"}</option><option value="MALE">{isArabic ? "ولد" : "Male"}</option><option value="FEMALE">{isArabic ? "بنت" : "Female"}</option></select><label><input name="operationsTeamLeader" type="checkbox" defaultChecked={employee.operationsTeamLeader} /> {isArabic ? "تيم ليدر" : "Team Leader"}</label><button>{isArabic ? "حفظ" : "Save"}</button></form>)}</section>
  </div>;
}

function TemplateSettings({ settings, isArabic, busy, saveSetting }) {
  const [template, setTemplate] = useState(settings.DAILY_OPERATIONS_TEMPLATE_CONFIG || {});
  const [phrases, setPhrases] = useState((settings.OPS_MOTIVATION_PHRASES || []).join("\n"));
  return <div className="ops-settings-section"><h2>{isArabic ? "تيمبلت العمليات" : "Daily operations template"}</h2><section className="grid two"><article className="panel form-grid"><Field label={isArabic ? "رابط اللوجو" : "Logo URL"}><input value={template.logoUrl || ""} onChange={(e) => setTemplate({ ...template, logoUrl: e.target.value })} /></Field><Field label={isArabic ? "اسم الفرع" : "Branch name"}><input value={template.branchName || ""} onChange={(e) => setTemplate({ ...template, branchName: e.target.value })} /></Field><Field label={isArabic ? "اللون الأساسي" : "Primary color"}><input type="color" value={template.primary || "#301848"} onChange={(e) => setTemplate({ ...template, primary: e.target.value })} /></Field><Field label={isArabic ? "لون التمييز" : "Accent color"}><input type="color" value={template.accent || "#f8c800"} onChange={(e) => setTemplate({ ...template, accent: e.target.value })} /></Field><Field label={isArabic ? "الملاحظات الافتراضية" : "Default notes"}><textarea aria-label={isArabic ? "الملاحظات الافتراضية" : "Default notes"} value={template.operationalNotes || ""} onChange={(e) => setTemplate({ ...template, operationalNotes: e.target.value })} /></Field><button disabled={busy} onClick={() => saveSetting("DAILY_OPERATIONS_TEMPLATE_CONFIG", template)}>{isArabic ? "حفظ التيمبلت" : "Save template"}</button></article><article className="panel"><h3>{isArabic ? "الجمل التحفيزية" : "Motivational phrases"}</h3><p className="muted">{isArabic ? "جملة واحدة في كل سطر، وتتغير يوميًا." : "One phrase per line, rotated daily."}</p><textarea aria-label={isArabic ? "الجمل التحفيزية" : "Motivational phrases"} className="ops-long-textarea" value={phrases} onChange={(e) => setPhrases(e.target.value)} /><button disabled={busy} onClick={() => saveSetting("OPS_MOTIVATION_PHRASES", phrases.split("\n").map((item) => item.trim()).filter(Boolean))}>{isArabic ? "حفظ الجمل" : "Save phrases"}</button></article></section></div>;
}

function MasterData({ config, settings, isArabic, busy, saveSetting, saveRecord }) {
  const [catalogues, setCatalogues] = useState(settings.OPS_PLANNING_CATALOGS || { meals: [], partyRooms: [], stockItems: [] });
  return <div className="ops-settings-section"><h2>{isArabic ? "البيانات الأساسية" : "Planning master data"}</h2><section className="grid two"><article className="panel"><h3>{isArabic ? "الأكاديميات والمدارس" : "Academies and schools"}</h3>{config.partners.map((item) => <div className="row" key={item.id}><b>{item.name}</b><span>{item.supervisorName || "—"} · {item.supervisorPhone || "—"}</span></div>)}<form className="form-grid" onSubmit={(event) => { event.preventDefault(); saveRecord("savePartner", event.currentTarget); }}><input name="name" required placeholder={isArabic ? "اسم الأكاديمية" : "Academy name"} /><input name="supervisorName" placeholder={isArabic ? "اسم المشرف" : "Supervisor"} /><input name="supervisorPhone" placeholder={isArabic ? "رقم المشرف" : "Phone"} /><textarea name="notes" placeholder={isArabic ? "ملاحظات" : "Notes"} /><button disabled={busy}>{isArabic ? "حفظ الأكاديمية" : "Save academy"}</button></form></article><article className="panel"><h3>{isArabic ? "عملاء أعياد الميلاد" : "Birthday customers"}</h3>{config.customers.map((item) => <div className="row" key={item.id}><b>{item.customerName}</b><span>{item.phone} · {item.childName || "—"}</span></div>)}<form className="form-grid" onSubmit={(event) => { event.preventDefault(); saveRecord("saveCustomer", event.currentTarget); }}><input name="customerName" required placeholder={isArabic ? "اسم العميل" : "Customer"} /><input name="phone" required placeholder={isArabic ? "رقم الهاتف" : "Phone"} /><input name="childName" placeholder={isArabic ? "اسم الطفل" : "Child"} /><textarea name="notes" placeholder={isArabic ? "ملاحظات" : "Notes"} /><button disabled={busy}>{isArabic ? "حفظ العميل" : "Save customer"}</button></form></article></section><section className="grid three"><Catalogue title={isArabic ? "الوجبات" : "Meals"} value={catalogues.meals || []} onChange={(meals) => setCatalogues({ ...catalogues, meals })} /><Catalogue title={isArabic ? "الغرف" : "Party rooms"} value={(catalogues.partyRooms || []).map((item) => typeof item === "string" ? item : item.name)} onChange={(rooms) => setCatalogues({ ...catalogues, partyRooms: rooms.map((name) => ({ name, active: true })) })} /><Catalogue title={isArabic ? "أصناف الستوك" : "Stock items"} value={catalogues.stockItems || []} onChange={(stockItems) => setCatalogues({ ...catalogues, stockItems })} /></section><button disabled={busy} onClick={() => saveSetting("OPS_PLANNING_CATALOGS", catalogues)}>{isArabic ? "حفظ الكتالوجات" : "Save catalogues"}</button><section className="panel"><h3>{isArabic ? "الإشعارات التشغيلية" : "Operational notices"}</h3>{config.notices?.map((item) => <div className="row" key={item.id}><span><b>{item.title}</b><small>{item.effectiveFrom} — {item.effectiveTo || "∞"}</small></span><em>{item.priority}</em></div>)}<form className="form-grid" onSubmit={(event) => { event.preventDefault(); saveRecord("saveNotice", event.currentTarget); }}><input name="title" required placeholder={isArabic ? "عنوان الإشعار" : "Notice title"} /><textarea name="message" required placeholder={isArabic ? "نص الإشعار" : "Notice message"} /><select name="priority" defaultValue="INFO"><option value="INFO">Info</option><option value="WARNING">Warning</option><option value="CRITICAL">Critical</option></select><input name="effectiveFrom" type="date" required /><input name="effectiveTo" type="date" /><button>{isArabic ? "حفظ الإشعار" : "Save notice"}</button></form></section></div>;
}
function Catalogue({ title, value, onChange }) { return <article className="panel"><h3>{title}</h3><textarea aria-label={title} className="ops-long-textarea" value={value.join("\n")} onChange={(event) => onChange(event.target.value.split("\n").map((item) => item.trim()).filter(Boolean))} /></article>; }

function RulesSettings({ settings, criteria, isArabic, busy, saveSetting }) {
  const [attendance, setAttendance] = useState(settings.OPS_ATTENDANCE_RULES || {}); const [evaluation, setEvaluation] = useState(settings.OPS_EVALUATION_RULES || {}); const [leave, setLeave] = useState(settings.OPS_LEAVE_RULES || {}); const [rotation, setRotation] = useState(settings.OPS_ROTATION_RULES || {});
  return <div className="ops-settings-section"><h2>{isArabic ? "قواعد التشغيل" : "Operation rules"}</h2><section className="grid two"><RuleCard title={isArabic ? "الحضور" : "Attendance"}><Field label={isArabic ? "سماح التأخير بالدقائق" : "Late grace minutes"}><input type="number" value={attendance.lateGraceMinutes || 0} onChange={(e) => setAttendance({ ...attendance, lateGraceMinutes: Number(e.target.value) })} /></Field><Field label={isArabic ? "سماح الخروج المبكر" : "Early-leave grace"}><input type="number" value={attendance.earlyLeaveGraceMinutes || 0} onChange={(e) => setAttendance({ ...attendance, earlyLeaveGraceMinutes: Number(e.target.value) })} /></Field><button disabled={busy} onClick={() => saveSetting("OPS_ATTENDANCE_RULES", attendance)}>{isArabic ? "حفظ" : "Save"}</button></RuleCard><RuleCard title={isArabic ? "التقييم" : "Evaluation"}><p>{criteria?.label || (isArabic ? "لا توجد نسخة معايير نشطة" : "No active criteria version")}</p>{criteria?.criteria?.map((item) => <div className="row" key={item.id}><b>{item.labelAr || item.label}</b><span>{item.maxScore}</span></div>)}<label><input type="checkbox" checked={evaluation.requireReasonForDeduction !== false} onChange={(e) => setEvaluation({ ...evaluation, requireReasonForDeduction: e.target.checked })} /> {isArabic ? "سبب الخصم إلزامي" : "Deduction reason required"}</label><button disabled={busy} onClick={() => saveSetting("OPS_EVALUATION_RULES", evaluation)}>{isArabic ? "حفظ" : "Save"}</button></RuleCard><RuleCard title={isArabic ? "الإجازات والوقت" : "Leave and overtime"}><Field label={isArabic ? "ساعات اليوم الإضافي" : "Overtime hours per day"}><input type="number" value={leave.overtimeHoursPerDay || 8} onChange={(e) => setLeave({ ...leave, overtimeHoursPerDay: Number(e.target.value) })} /></Field><Field label={isArabic ? "تعويض العمل في العطلة" : "Holiday work credit"}><input type="number" value={leave.holidayWorkCreditDays || 2} onChange={(e) => setLeave({ ...leave, holidayWorkCreditDays: Number(e.target.value) })} /></Field><button disabled={busy} onClick={() => saveSetting("OPS_LEAVE_RULES", leave)}>{isArabic ? "حفظ" : "Save"}</button></RuleCard><RuleCard title={isArabic ? "الروتيشن" : "Rotation"}><Field label={isArabic ? "خلايا كل شيفت" : "Slots per shift"}><input type="number" value={rotation.slotsPerShift || 8} onChange={(e) => setRotation({ ...rotation, slotsPerShift: Number(e.target.value) })} /></Field><Field label={isArabic ? "الروتيشنز الإلزامية (كود ومفصولة بفاصلة)" : "Mandatory position codes (comma separated)"}><input value={(rotation.mandatoryPositionCodes || []).join(", ")} onChange={(e) => setRotation({ ...rotation, mandatoryPositionCodes: e.target.value.split(",").map((item) => item.trim().toUpperCase()).filter(Boolean) })} /></Field><label><input type="checkbox" checked={rotation.preserveManualLocks !== false} onChange={(e) => setRotation({ ...rotation, preserveManualLocks: e.target.checked })} /> {isArabic ? "حماية التعديلات اليدوية" : "Preserve manual locks"}</label><button disabled={busy} onClick={() => saveSetting("OPS_ROTATION_RULES", rotation)}>{isArabic ? "حفظ" : "Save"}</button></RuleCard></section></div>;
}
function RuleCard({ title, children }) { return <article className="panel form-grid"><h3>{title}</h3>{children}</article>; }
function Safety({ health, isArabic, reload }) { return <div className="ops-settings-section"><h2>{isArabic ? "السجل والسلامة" : "Audit and safety"}</h2><div className="grid three metrics"><article className="card metric"><div className="label">SQLite</div><div className="value">{health?.database?.integrity || "…"}</div></article><article className="card metric"><div className="label">{isArabic ? "حجم القاعدة" : "Database size"}</div><div className="value">{health ? `${(health.database.sizeBytes / 1024 / 1024).toFixed(1)} MB` : "…"}</div></article><article className="card metric"><div className="label">{isArabic ? "النسخ" : "Backups"}</div><div className="value">{health?.backups?.length || 0}</div></article></div><section className="grid two"><article className="panel"><h3>{isArabic ? "أحدث النسخ" : "Latest backups"}</h3>{health?.backups?.map((item) => <div className="row" key={item.name}><b>{item.name}</b><span>{(item.sizeBytes / 1024 / 1024).toFixed(1)} MB</span></div>)}</article><article className="panel"><h3>{isArabic ? "آخر تغييرات العمليات" : "Latest operations changes"}</h3>{health?.recentOperations?.map((item) => <div className="row" key={item.id}><span><b>{item.summary}</b><small>{item.user}</small></span><time>{new Date(item.createdAt).toLocaleString()}</time></div>)}</article></section><button onClick={reload}>{isArabic ? "إعادة الفحص" : "Run health check"}</button></div>; }
